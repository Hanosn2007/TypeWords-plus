# 2026-09-22 账号与数据版本

源码标签：`release/2026-09-22`；上一上线标签：`release/2026-09-21`。Git标签只保存代码，不包含或回退学习数据。

统一“账号与数据”入口，提供本站账号同步、完整JSON/ZIP导入导出、本机离线恢复和来源独立的历史管理。固定北京时间每日仅最新一份；本机默认3个有效日期、可调1–30，云端3个。恢复先提交本机，云端版本冲突人工比较，不自动选更大词数。整份替换与附件使用同一事务，失败显式回滚。

保留词书/单元ID、内容固定版本、FSRS、统计、v3正式与自由任务。学习数据格式版本未提高，前端发布提示版本由3变为4。旧上游账号、Supabase后台同步及VSCode客户端退役；原作者署名、许可证和Git历史保留。

发布检查包括核心测试、Go测试/vet、完整网页构建、真实浏览器及服务器数据库独立副本验证。全仓类型检查仍有既有诊断（89条输出，含不同路径重复），不宣称类型检查全部通过。用户实际输入手感、长期离线和物理多设备体验继续验收。详见 `ACCOUNT_DATA_ACCEPTANCE.md`。

本次发布需要独立保存当前正式浏览器数据、云端只读快照、服务器SQLite和旧前端/API，并在切换前后核对原表内容。测试库不上传，旧学习库不恢复。周期服务器/Mac备份继续关闭，发布结果与哈希记录在私有 `output/releases/20260922T044811Z/`。
