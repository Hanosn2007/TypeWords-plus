# 同步保护工程

2026-09-16更新：下文9月16日候选改动已发布为20260916T051939Z；备份与发布证据见output/releases/20260916T051939Z/RELEASE.md。周期双机备份继续关闭。

范围：保留现有词书、单元学习、FSRS、账号和管理功能；替换自建CloudSync的数据同步路径。Supabase历史客户端不属于这次新协议。已随20260914T125642Z联合发布；周期双机备份按用户决定保持关闭。

## 同步约定

- 词书、设置、单词任务、文章任务作为同一账号的完整快照，服务器单事务写入。公共词书内容仍由library接口维护，个人同步不修改书库发布记录。
- 客户端保存已确认的版本号和本机基线。单边修改正常同步；双方修改时保留双方，由用户选择完整版本。不按客户端时钟选胜者，不擅自合并FSRS或取最大词数。
- 写入必须携带expectedRevision和持久化requestId。服务器拒绝过期版本；断网/响应丢失用同一个请求重试，避免重复提交。账号归属改变先确认，不自动把前一个账号的数据上传给后一个账号。
- 自动同步不替换活跃学习现场。非练习页首次加载时，确认本机无改动后可自动下载；活跃页面远端变化在同步页处理。首次登录本机为空时可在保存副本后下载。冲突页确认前再次核对本机签名和云端版本。
- 同一浏览器同一站点只允许一个可写页面。第二个标签页显示明确提示，不让两个内存状态竞争同一份IndexedDB。
- 旧/sync/data和/sync/meta读写均返回428，不允许仍打开的旧网页绕过版本校验或把旧下载覆盖本机。更新时必须先保全浏览器数据；不能只更新前端却声称新协议已启用。

## 恢复与备份

- 9月16日本地候选版增加正常关闭提醒：本机写入未完成/失败，或云端处于待同步、同步中、错误、冲突状态时，beforeunload触发浏览器原生确认。用户选择留下后正常继续同步；页面区分本机保存与云端同步。浏览器无法可靠区分刷新和关闭，因此刷新也可能提示，但已落盘本机数据不会因刷新而清空。处理器只读状态，不做全量扫描或备份；不承诺浏览器退出前网络一定完成。

- 9月16日本地候选版（尚未发布）：本机仅保留最近3份关键操作恢复副本，在登录、下载、冲突处理/恢复等覆盖前创建；普通练习上传不再另做全量备份。备份失败仍中止覆盖。旧生产版的20份/定期备份策略将在此次发布后替换，已有副本在下次创建时按新上限裁剪。
- 服务器gzip保存被覆盖前的完整快照，按账号隔离。保留72个小时桶、30个日桶、最近20次冲突处理和20次恢复前版本。桶只在发生写入时生成，不代表连续每小时都有记录。
- 历史恢复作为新版本提交，并备份恢复前状态；不回滚整库、不修改其他账号。
- 9月16日本地候选版：取消定时同步及定时失败重试，窗口失焦/页面隐藏时触发，恢复网络或本地保存完成时仅在失焦状态尝试；手动同步仍立即执行。快速切回时尚未开始的自动任务跳过，已发出的请求正常完成。按单词/阶段的本地保存不变，关闭页面不保证云端上传完成。快照先剔除可下载内容再复制，签名分片让出主线程且保持字节兼容；服务器已有历史桶时跳过重复压缩。版本冲突仍交由人工选择，不自动覆盖或合并。
- server/backup_sync.py执行SQLite在线备份、独立副本恢复验证、压缩归档和SHA256；不记录凭据。每日timer补跑错过的任务。异机位置通过TYPEWORDS_BACKUP_OFFSITE配置已有SSH连接；未配置时报告offsiteCopied=false，不冒充异机备份。
- 完整归档暂不自动删除，需观察实际体积后配置存储保留；避免未经确认清除恢复来源。
- 后续异机目标保留为此Mac，但用户已明确暂时关闭双机周期备份。scripts/pull-sync-backups.py和launchd模板保留；独立密钥只在本机准备，未添加服务器公钥、未装载任何定时任务。TYPEWORDS_SCHEDULED_BACKUPS默认false，管理页显示关闭。本机覆盖前副本及云端有限历史恢复仍启用。

## 发布与回退边界

- 生产为20260914T125642Z完整同步保护版，发布证据见output/releases/20260914T125642Z/RELEASE.md。以后新发布仍按当次授权。
- 发布前备份当前完整数据库、服务程序、前端及浏览器现场，先在独立数据库副本跑迁移。新表迁移只新增sync_state/sync_receipts/sync_history，不改旧学习数据。
- 回退只回退程序；不得恢复旧数据库覆盖期间新增学习记录。若回退到不支持版本校验的旧API，需要保持旧写接口禁用，否则会重新开放覆盖风险。
- 仍须实机验收实际练习、跨浏览器操作和断网重连；自动化验证不能替代用户体验验收。
