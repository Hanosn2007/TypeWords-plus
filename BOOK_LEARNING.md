# 独立词书学习与重复词提示

实现日期：2026-09-06。当前为本地实现，未发布到线上。

## 使用方式

- 单词首页顶部可直接切换词书，切换后更新该书的进度和任务；点击开始或继续学习即可进入，不再经过学习设置弹窗。
- 首页“当前词书设置”和练习底栏“词书设置 → 当前词书”可修改重复词策略、每轮新词与复习比例。数量设置保存后立即应用：尚未作答或本轮已完成时直接重建；本轮已作答但未完成时，先确认是否清除整轮现场，取消保留原设置和进度。已结算记录与掌握状态保留；仅修改重复词策略不重建练习。
- 同一个账号中的每本词书分别保存学习进度、练习模式、复习比例、FSRS 卡片、已掌握词、跳过词和未完成练习。
- 从词书详情开始学习或在单词首页继续，可以 A → B → A 切换；练习现场按词书恢复。
- 词书的学习设置提供三种重复词策略：不提示、提示后手动跳过（默认）、自动跳过其他词书已掌握的词。
- 提示区显示其他词书名称，并区分“学过”和“已掌握”。正常输入仍可重复学习。
- 设置页可以修改“跳过其他词书已学词”的快捷键，默认空格。快捷键只在尚未作答时触发；输入中仍可点击跳过按钮。
- 跳过仅影响当前词书，不会把词标成已掌握，也不会计入本轮新学或复习数；结算单列跳过数。
- 可以撤销最近一次跳过，包含最后一个词跳过后的结算页。开始下一次作答后不再保留这一临时撤销点。
- 批量自测支持逐行跳过/撤销。自动模式只预选其他词书明确已掌握的词，提交前仍可撤销。
- 学习设置中“恢复跳过词”会清空当前书的跳过列表，把进度退回最早被跳过的位置，并切回手动模式。之后可能再次遇到该位置以后的已学词。

## 数据与兼容

`Dict.learning` 保存词书级的学习记录。旧全局 FSRS 原样保留；只有词书已加载且有完成进度时，才复制相应已完成范围中的历史卡片，各书复制后的卡片相互独立。旧全局已掌握词只迁移到首次实际加载的当前词书，不据此标记所有其他词书。旧数据无法恢复当时未记录的精确词书来源。

`practice_word` 同步载荷升级为 `schemaVersion: 2` 的词书条目集合，每个条目有自己的更新时间；清除一个练习写入该条目的删除标记，保留其他词书。旧单份缓存按 dictId 迁移，无 dictId 的本地旧缓存只在启动恢复旧数据时绑定到原先选中的词书，之后切书不会重新认领。备份导出包含整套词书练习缓存。

复用现有账号同步通道。普通上传先读取远端并逐词书合并，网络读取失败时保留待同步数据重试；本地保存与远端合并写入排队，防止网络等待期间的新本地进度被旧快照覆盖。

现有服务端仍是整行最后写入生效，未增加服务端事务合并：两台设备完全同时读取旧数据再上传，仍有覆盖风险。词书主数据也沿用已有整份同步/冲突机制，不能把本次客户端合并视为任意并发编辑保证。真实账号的双设备同步尚需验收。

## 验证

- Node 内置测试：`node --test packages/core/test/bookLearning.test.ts packages/core/test/practiceWordCacheBundle.test.ts`，9 项通过。
- 浏览器使用隔离 Chrome 会话和虚构词书，不修改真实账号数据。
- 已验证 A → B → A 续接，IndexedDB 同时保留两本词书缓存。
- 已验证空格跳过、最后一词跳过不生成记忆卡片、跳过独立统计、结算后撤销恢复进度与统计。
- 已验证整词输入中空格保持输入功能、空白答案时空格跳过、自定义 F4 跳过。
- 已验证自动模式只跳过其他词书已掌握的词，学过但未掌握的词保留；撤销后不会立即再次自动跳过。
- 已验证批量自测的自动预选、逐词跳过和结算：跳过词不计入已学、已掌握或 FSRS。
- Nuxt 生产静态构建通过；VS Code Web 兼容构建通过。
- Nuxt 类型诊断与改动前基线比较无新增诊断；仓库仍有既有类型错误，不能声称全仓类型检查通过。
- 视觉、听感、跨设备实际同步由用户验收。
