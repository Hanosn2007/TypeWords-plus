# 单元词书 JSON

个人版通过「导入单词 → 新建空词书 → 上方上传文件」导入带单元的 JSON。
原有 TXT、单词数组 JSON 和 Excel 仍可用；没有单元信息的文件按整书导入，不猜测每课词数。

```json
{
  "format": "typewords-units",
  "version": 1,
  "name": "我的词书",
  "units": [
    {
      "id": "lesson-01",
      "name": "Lesson 1",
      "words": ["apple", "banana"]
    },
    {
      "id": "lesson-02",
      "name": "Lesson 2",
      "words": [
        "banana",
        {
          "word": "pacific",
          "phonetic0": "pə'sɪfɪk",
          "trans": [{ "pos": "adj.", "cn": "和平的；平静的" }],
          "sentences": [
            { "c": "He was a very pacific person.", "cn": "他是一个性格温和的人。" }
          ]
        }
      ]
    }
  ]
}
```

- `units` 的顺序就是单元顺序，`words` 的顺序就是课内顺序。每课词数可以不同。
- 单元 `id` 在同一本书内唯一；省略时按顺序生成 `unit-01` 等标识。`name` 为显示名称。
- 词条可写字符串，让作者词库匹配；也可写带 `word`、`trans`、音标、例句等的对象，优先保留文件提供的内容。
- 同一个词可出现在多个单元，单元成员关系全部保留；同一本书里共用该词的内容、学习记录和 FSRS。
- 查不到的词仍保留在原单元，导入结果列出缺释义词，可在词书详情编辑补充。请求失败会提示重试，不把网络错误伪装成全部缺词。
- 带单元的文件当前只导入新建的空词书，避免覆盖已有学习记录。词书名称沿用导入时选择/创建的名称；JSON 的 `name` 是文件说明信息。
- 每本词书保留一个未完成的练习；本轮未完成时先继续练习，完成后再切换单元。新词不越过所选单元边界，复习沿用整本书的安排。
- 词书设置可选「跟随单元 / 自定义数量」。有单元的词书默认安排所选单元全部剩余新词；选择整本词书时使用自定义数量。复习仍以自定义数量和复习比例安排。数量设置保存后立即应用；已作答且本轮未完成时先确认是否重新开始，取消则保留原设置和现场。尚未作答或本轮已完成时不弹警告。
- 单元成员、所选单元和已处理记录随词书保存，练习现场包含本轮单元。沿用现有同步服务；完全同时跨设备写入的原有冲突风险尚未解决。

已导入的 Vocabulary 6000 可在首页点击「启用 31 个单元」，无需删书重导：程序核对原书成员后建立 Lesson，并补齐缺失的 pacific、renaissance、virgin；原书顺序和已有学习记录分别保留。

## 本阶段边界

当前为个人使用版本。作者源与自有公共增量词库、标准词书版本发布、年级账号权限、反馈审核及集中使用验证列为后续任务，见 PROJECT_LOG.md 的 L-20260910-01。
